<nav class="sticky top-0 z-50 glass-effect border-b border-slate-100 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-20">
            <div class="flex items-center">
                <a href="/" class="flex items-center space-x-3 group">
                    <div class="bg-indigo-600 p-2 rounded-xl group-hover:rotate-12 transition-soft">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"></path>
                        </svg>
                    </div>
                    <span class="text-2xl font-bold tracking-tight text-slate-800">Akbir<span class="text-indigo-600">Parts</span></span>
                </a>
            </div>
            
            <% if (locals.user && user.userId) { %>
            <div class="flex items-center space-x-6">
                <% if(user.role === 'customer') { %>
                    <a href="/customer" class="text-sm font-medium text-slate-600 hover:text-indigo-600 transition-soft">Belanja</a>
                    <a href="/customer/history" class="text-sm font-medium text-slate-600 hover:text-indigo-600 transition-soft">Riwayat</a>
                <% } %>
                
                <div class="flex items-center pl-6 border-l border-slate-200">
                    <div class="mr-4 text-right">
                        <p class="text-xs font-bold text-slate-400 uppercase tracking-widest"><%= user.role %></p>
                        <p class="text-sm font-semibold text-slate-800"><%= user.userName %></p>
                    </div>
                    <div class="relative group">
                        <button class="w-10 h-10 rounded-full bg-indigo-100 flex items-center justify-center text-indigo-600 border-2 border-white shadow-sm ring-1 ring-slate-100">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"></path></svg>
                        </button>
                        <div class="absolute right-0 mt-2 w-48 bg-white rounded-2xl shadow-xl border border-slate-100 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-soft z-50">
                            <div class="p-4 border-b border-slate-50">
                                <p class="text-xs text-slate-400">Masuk sebagai</p>
                                <p class="text-sm font-bold truncate"><%= user.userName %></p>
                            </div>
                            <a href="/auth/logout" class="block w-full text-left px-4 py-3 text-sm text-red-600 hover:bg-red-50 transition-soft rounded-b-2xl">
                                Logout
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>
        </div>
    </div>
</nav>
